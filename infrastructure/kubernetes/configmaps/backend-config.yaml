apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: durga-puja
data:
  NODE_ENV: "production"
  PORT: "5000"
  # Database and Redis endpoints will be injected from secrets
  CLOUDINARY_CLOUD_NAME: "your-cloud-name"
  LOG_LEVEL: "info"
  CORS_ORIGIN: "*"
